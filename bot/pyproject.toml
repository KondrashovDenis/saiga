[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# E712 — false positive: SQLAlchemy "Column == True" это валидный SQL filter,
# а не Python boolean check. Linter не различает.
ignore = ["E712"]
